<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/LinearLayout1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/back"
    tools:context=".MainActivity" >

    <TextView
        android:id="@+id/menu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="16dp"
        android:layout_marginTop="16dp"
        android:gravity="fill_horizontal"
        android:textSize="40sp"
        android:textStyle="bold" />

    <TableLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:stretchColumns="*" >

        <TableRow>

            <Button
                android:id="@+id/music"
                android:onClick="startActivityMusic"
                android:text="@string/music"
                android:drawableRight="@drawable/next"
                android:drawableLeft="@drawable/music" />
        </TableRow>

        <TableRow>

            <Button
                android:id="@+id/events"
                android:onClick="startActivityEvents"
                android:text="@string/events"
                android:drawableRight="@drawable/next" 
                android:drawableLeft="@drawable/event"/>
        </TableRow>

        <TableRow>

            <Button
                android:id="@+id/merchandise"
                android:onClick="startActivityMerchandise"
                android:text="@string/merchandise"
                android:drawableRight="@drawable/next" 
                android:drawableLeft="@drawable/merch" />
        </TableRow>

        <TableRow>

            <Button
                android:id="@+id/literature"
                android:onClick="startActivityLiterature"
                android:text="@string/literature"
                android:drawableRight="@drawable/next"
                android:drawableLeft="@drawable/lit"  />
        </TableRow>

        <TableRow>

            <Button
                android:id="@+id/radio"
                android:onClick="startActivityRadio"
                android:text="@string/radio" 
                android:drawableRight="@drawable/next"
                android:drawableLeft="@drawable/radio" />
        </TableRow>
    </TableLayout>
</LinearLayout>